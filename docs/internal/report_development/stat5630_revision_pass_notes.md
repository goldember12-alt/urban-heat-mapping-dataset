# STAT 5630 Final Report Revision Pass Notes

Date: 2026-05-03

## Revision Checklist Used

- Reduce repeated framing around same-city screening, city-held-out transfer, exact-cell retrieval, broad spatial placement, sampled benchmarking, and limitations.
- Define local terms earlier and use plainer language afterward.
- Keep all reported metrics unchanged unless verified from existing artifacts.
- Improve Figure 4 and Figure 5 interpretation through clearer titles/captions rather than changing their underlying metrics.
- Promote the all-city spatial-alignment summary from the appendix into the main figure set.
- Fix the selected Nashville/San Francisco spatial-alignment contrast so it is a single readable appendix figure rather than two separate map images that can split across pages.
- Keep limitations, but make them proportional and easier to read.
- Expand future work with concrete next steps.

## What Changed

- Revised `docs/report/stat5630_final_report_draft.md` for a controlled prose pass:
  - tightened the background so it reaches the research problem faster;
  - defined same-city screening, exact-cell transfer, and broad spatial placement near the start;
  - condensed the research questions from five secondary questions to three;
  - clarified the 2 km buffer as a pragmatic fixed design rule;
  - added a concrete explanation of how `hotspot_10pct` is constructed after filtering;
  - clarified average precision (AP) as the reported PR-ranking summary;
  - reframed the 70/30 within-city artifact as a diagnostic contrast rather than a fully reproducible leaderboard;
  - split the old combined results/conclusion/limitations section into `Results and Discussion`, `Limitations and Future Work`, and `Conclusion`;
  - reduced repeated caveats while preserving the same statistical claims.
- Revised captions and figure usage:
  - shortened and sharpened Figure 4 caption language;
  - renamed Figure 5 to emphasize the weak same-city/transfer relationship;
  - tightened Figure 6 caption;
  - promoted former Appendix Figure A7 to main Figure 7;
  - replaced former Appendix Figure A8 with a single combined high/low map contrast.
- Updated `src/report_artifacts.py`:
  - added `plot_selected_spatial_alignment_map_contrast(...)`;
  - wired `src.run_report_artifacts` to regenerate `docs/report/figures/selected_spatial_alignment_map_contrast.png`.
- Updated `tests/test_report_artifacts.py`:
  - added coverage for the new combined spatial-alignment map contrast.

## What I Intentionally Did Not Change

- Did not change any reported benchmark metrics, city counts, fold counts, row counts, or spatial-alignment metric values.
- Did not rerun modeling or create new model results.
- Did not remove limitations; the pass made them less repetitive and more proportional.
- Did not redesign Figure 4 from scratch because it is presentation-derived and still useful with clearer surrounding text.
- Did not relabel every Figure 5 city or rebuild that figure because the existing labeled version had already been rendered and checked for legibility.
- Did not simplify Table 1 structurally in code; the main improvement in this pass was caption/text context, not table redesign.

## Artifacts Regenerated

Command run:

```powershell
C:\Users\golde\.venvs\STAT5630_FinalProject_DataProcessing\Scripts\python.exe -m src.run_report_artifacts
```

Affected/new report-facing artifacts include:

- `docs/report/figures/selected_spatial_alignment_map_contrast.png`
- `docs/report/figures/spatial_alignment_medium_summary.png`
- `docs/report/figures/heldout_denver_map_focus.png`
- existing report tables and figures regenerated by `src.run_report_artifacts`

PDF render command run from `docs/report/`:

```powershell
C:\Users\golde\AppData\Local\Programs\Quarto\bin\tools\pandoc.exe stat5630_final_report_draft.md --from markdown+pipe_tables+raw_tex+link_attributes-implicit_figures --to pdf --standalone --pdf-engine=xelatex -V geometry:margin=1in -V fontsize=12pt -V papersize=letter -o stat5630_final_report_draft.pdf
```

## Verification

- `python -m py_compile src\report_artifacts.py tests\test_report_artifacts.py` passed.
- `python -m pytest tests\test_report_artifacts.py -q` passed: `3 passed`.
- `python -m src.run_report_artifacts` passed and wrote the new combined map contrast figure.
- Pandoc/XeLaTeX PDF render passed.
- Markdown image-reference check found `14` image references and no missing image files.

## Remaining For Later Pass

- A full visual QA pass on the rendered PDF would still be useful, especially for Figure 7 and Appendix Figure A7.
- Table 1 could still be simplified into a more polished report table if time allows.
- Figure 4 could be rebuilt as a report-native figure with an explicit 0.10 reference line in the transfer panel.
- A later pass could further reduce Figure 5 clutter by labeling only selected cities, but that would require regenerating and rechecking the figure.
